{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: events for progress and drag-and-drop, window dragging for the custom title bar.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
